<div class="container mt-3 mb-4">
  <h3><%= t("messages.welcome_back") %>, <%= current_user.email.gsub(/@.*$/, "").capitalize %><%="様" if I18n.locale == :ja %>!</h3>
</div>
<div class="container-fluid mt-4">
  <div class="row mb-4">
    <!-- Left grid -->
    <div class="col-md-8">
      <div class="row row-cols-3 g-3">
        <% 8.times do %>
          <div class="col">
            <div class="card category-card text-center h-100 border-0 py-4">
              <div class="card-body d-flex flex-column justify-content-center align-items-center">
                <i class="fa-solid fa-desktop fa-3x"></i>
                <p class="card-text mt-2 fs-5">electronics</p>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
    <!-- Right panel -->
    <div class="col-md-4">
      <div class="card bg-light border-0" style="max-height: 80vh; overflow-y: auto;">
        <div class="card-body">
          <h5 class="card-title">Pending:</h5>
          <% 5.times do  %>
            <div class="pending-item mb-3 pb-3 border-bottom">
              <div class="d-flex justify-content-between align-items-start">
                <strong>#{offer_title}</strong>
                <span class="badge rounded-pill bg-danger">7 days ago</span>
              </div>
              <p class="mb-1">Kanto - Tokyo</p>
              <p class="mb-1"><strong>Condition:</strong> Used - Good</p>
              <p class="mb-1"><strong>Can ship by:</strong> Jul 3</p>
              <p class="mb-0"><strong>Alerts:</strong> Shipping info needed</p>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>

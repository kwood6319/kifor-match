# TODO: page title for single offer details
<% content_for :title, "Offer Details" %> 
# TODO: show request title and request description
<%= request.title %>
<%= request.description %>
# TODO: show charity name related to request
<%= request.charity.org_name %>
# TODO: show donor display_name related to offer
<%= offer.donor.display_name %>
# TODO: show quantity_offered with request units
<%= offer.quantity_offered %>
<%= request.units %>
# TODO: show offer condition
<%= offer.condition %>
# TODO: show donor message
<%= offer.donor_message %>
# TODO: show can_ship_by date
<%= offer.can_ship_by %>
# TODO: show tracking_number if present
<%= offer.tracking_number %>
# TODO: show current status clearly
<%= offer.status %>
# TODO: if charity owns request and offer is pending, show Approve button

# TODO: if charity owns request and offer is pending, show Reject button

# TODO: if charity owns request and offer is approved/shipped, show Mark as Recieved button

# TODO: add back link to offers index

# TODO: optionally show request quantity_remaining for context

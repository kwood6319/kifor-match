#TODO: page title for offers page (list)

#TODO: if current user is donor, show only thier offers with request title, quantity_offered, status, can_ship_by

#TODO: if current user is charity, show offers related to their request (or all offers?)

#TODO: display request infor for each offer (request title, charity, units, quantity_remaining if useful???)

#TODO: display donor infor for each offer (display_name, region, city) only when charity or admin is viewing

#TODO: show offer status badge (pending / approved / rejected / shipped / recieved)

#TODO: add link/button to view offer details 

#TODO: if charity owns the related request and offer is pending, show approve button

#TODO: if charity owns the related request and offer is pending, show reject button

#TODO: if charity owns the related request and offer is approved/shipped, show mark as received button

#TODO: if donor owns the offer and status allows it , show edit/mark shipped action later if implemented

#TODO: if admin, show all offers with related donor and charity info

#TODO: add empty state message when no offers exist 

#TODO: add search form button at top if search action/view is used


